# npm 12+ defaults allow-git=none.
# Passport pins @fabric/core and @fabric/http from GitHub; http also depends on
# core as git. Nested git dep preparation resolves those to commit SHAs —
# `allow-git=root` refuses that fetch. Use `all` for this stack.
allow-git=all
