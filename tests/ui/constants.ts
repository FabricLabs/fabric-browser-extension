'use strict';

/** BIP39 test vector — valid English wordlist phrase (do not use with real funds). */
export const TEST_MNEMONIC_ABANDON =
  'abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about';
